[package]
name = "tee-attestation-verification-ffi-utils"
version = "1.0.2"
edition = "2021"
repository = "https://github.com/microsoft/TEE-Attestation-Verification"
keywords = ["confidentiality", "sev-snp", "ffi"]
description = "Shared FFI types and constants for TEE attestation verification"
authors = ["Chris Jensen"]
license = "MIT"

[dependencies]

[target.'cfg(target_family = "wasm")'.dependencies]
wasm-bindgen = "0.2"
